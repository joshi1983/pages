class Point {
	/*
	@param position: should be an Array of length 2 with 2 numbers.
	@param colour: an Array of length 3 of red, green, blue value numbers.
	*/
	constructor(position, colour) {
		this.position = position;
		this.colour = colour;
	}
}