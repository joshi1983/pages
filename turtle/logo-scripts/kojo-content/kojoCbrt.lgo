; Similar to the math.cbrt function in Kojo and Scala
to kojoCbrt :val
	localmake "sign sign :val
	; isolate the sign so we don't run into problems with taking a root of a negative number.
	output :sign * pow abs :val 1/3
end