to pCircle :x :y :size
	ifelse or :ellipseMode = "center :ellipseMode = "radius [
		jumpTo [:x :y]
	] [
		jumpTo [:x + :size / 2 :y - :size / 2]
	]
	ifelse :ellipseMode = "radius [
		circle :size / 2
	] [
		circle :size
	]
end