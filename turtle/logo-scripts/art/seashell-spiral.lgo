setfillcolor transparent
repeat 80 [
	pendown
	make "fdSize power 1.06 repcount
	setpensize :fdSize * 0.1
	arcleft 30 :fdSize * 2
	left 110
	make "arcSize :fdSize * 1.5
	make "previousPos pos
	arcRight 40 :arcSize
	penup
	setpos :previousPos
	pendown
	right 69.9
]