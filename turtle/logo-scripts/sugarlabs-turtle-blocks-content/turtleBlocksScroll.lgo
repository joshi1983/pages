; Similar to the scroll block at:
; https://turtle.sugarlabs.org
;
; Sugarlabs Turtle Blocks scroll appears to move the previously drawn shapes.
; WebLogo can't move previously drawn shapes but WebLogo can move the turtle.
; Moving the turtle will sometimes lead to a similar drawing so that is what this procedure does.
to turtleBlocksScroll :xOffset :yOffset
	setPos [xCor + :xOffset yCor + :yOffset]
end