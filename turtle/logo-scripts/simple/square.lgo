setPenSize 5

; Draw the 4 sides in a loop.
repeat 4 [
	forward 200
	right 90
]

closePath
; closePath = connect the start and end points.
; Without closePath, the square looks good
; but just leaves a tiny opening at [0 0].