; Returns a string with the specified number of tab characters.
; This is similar to the tab function from QBasic
to qbTab :numTabs
	localmake "result ''
	repeat :numTabs [
		localmake "result word '	' :result
	]
	output :result
end