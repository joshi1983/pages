; Returns a string with the specified number of spaces.
; This is similar to the space$ function from QBasic
to qbSpace :numSpaces
	localmake "result ''
	repeat :numSpaces [
		localmake "result word ' ' :result
	]
	output :result
end