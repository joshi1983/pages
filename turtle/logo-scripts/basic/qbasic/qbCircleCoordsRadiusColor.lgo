to qbCircleCoordsRadiusColor :coords :radius :color
	if :radius > 0 [
		localmake "oldState turtleState
		jumpTo [first :coords -last :coords]
		setPenColor :color
		circle :radius
		setTurtleState :oldState
	]
end