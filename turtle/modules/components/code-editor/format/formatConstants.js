const tabSpaceEquivalent = 4;
const maxLineLength = 64;

/* 64 is a little shorter than some other 
programming language style guides but WebLogo might be used in a 
narrow viewport or smaller computer screen.
Shorter lines scroll less and become more readable if there is less width to view the code.
*/

// 79 is the length limit in Python's PEP-8 code styling guide.
// https://www.python.org/dev/peps/pep-0008/#maximum-line-length


export { tabSpaceEquivalent, maxLineLength };