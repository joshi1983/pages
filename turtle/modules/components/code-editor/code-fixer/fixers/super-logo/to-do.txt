DONE: 1. fix stuff like:
MAKE :X :X + 40
to
MAKE "X :X + 40

There should be a fixer for this.

2. rename hyphenated identifiers.
For example, "to line-of-dots" "to lineOfDots".

DONE: 3. fix brackets for instruction lists.
For example:
REPEAT 7 (FD 10)
to
REPEAT 7 [FD 10]

Apply similar fixes to brackets for while, if.

4. Process all "removeInMigration" and "to" from the migration JSON file.

DONE: 5. Fix "else" keyword usage.
For example,
 IF :x = "F ( PRINT "RIGHT)
ELSE (PRINT "WRONG)

should become:
 ifelse :x = "F [ PRINT "RIGHT]
[PRINT "WRONG ]

DONE: 6. Fix usage of test, iftrue, iffalse.  There should be an existing fixer for this.  Just include the fixer.

DONE: 7. Use fixClashingNames fixer or similar if a program defines a procedure named "circle" for example.

8. 
    

