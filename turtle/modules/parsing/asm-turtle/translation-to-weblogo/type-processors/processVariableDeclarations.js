export function processVariableDeclarations(token, result) {
	// Don't generate anything here because WebLogo has no equivalent to variable declarations.
	// The variables are useless until they're assigned a value so that's the part 
	// where commands like make or localmake will be used.
};