WebLogo currently depends mostly on DT Python Parser for Python parsing.

This folder contains a start at a parser for Python that is made completely for WebLogo.
This is a work in progress to reduce dependence on a third-party library that contains
bugs that are not getting fixed quickly enough.  One known bug can freeze its lexical analyzer.
