The Shader Viewer is a prototype to
experiment with a potential download/export for 
Logo programs that have procedural images.

TO DO:
- Write a function to compute a colour from
a specified x and y coordinate.
	- a julia set or mandelbrot fractal function sounds good.
- Test the function by drawing on the canvas.
- Create a worker for computing colours and returning them.
- Add a zoom feature.
	- trigger zoom in and out using the keyboard.
	- trigger zoom by listening to the mouse wheel.
- Add a feature for exploring around.