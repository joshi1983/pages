This directory is for an experiment on making a worker 
that can work even if the page is loaded directly 
into a web browser from the file system.

This can help a shader exporter download a more easily run shader viewer.