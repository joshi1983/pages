This is a work in progress.  This is a new parser to replace the other code that depends on the DT Python Parser library.

When this new parser finally replaces the old one, the following should be cleaned up:
- ParseTreeTokenType.NOT, ParseTreeTokenType.PRINT, translation-to-weblogo/processNotToken