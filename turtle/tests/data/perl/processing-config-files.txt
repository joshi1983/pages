#!/usr/bin/perl
use strict;
use warnings;

# copied from: https://learn.perl.org/examples/configuration_files.html

use Config::Any;

my @files = (
        'path/to/config_file.json',
        'path/to/config.pl',
        'path/to/config.xml'
);

my $config = Config::Any->load_files( { files => \@files } );