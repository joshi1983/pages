#include "./some-file.HC"

// copied from: https://holyc-lang.com/docs/intro
class Person
{
  U8 name[SOME_FILE_CONSTANT];
  I64 age;
};

Person *PersonNew(U8 *name, I64 age)
{
  Person *p = MAlloc(sizeof(Person));
  MemCpy(p->name,name,sizeof(p->name));
  p->age = age;
  return p;
}

Person *p1 = PersonNew("Brian", 45);
"%s %d\n",p->name,p->age;