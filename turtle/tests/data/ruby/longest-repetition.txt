# copied from: https://www.rubyguides.com/2019/02/ruby-code-examples/
def longest_repetition(string)
  max = string
          .chars
          .chunk(&:itself)
          .map(&:last)
          .max_by(&:size)
  max ? [max[0], max.size] : ["", 0]
end
longest_repetition("aaabb")
# ["a", 3]