!  SGFunc1   Graph the function "Sin(x*x)".

LIBRARY "SGFunc.trc"
LIBRARY "SGLib.trc"

CALL SetText ("Sin(x*x)", "X Values", "Y Values")
CALL Fgraph (-pi, pi, 2, "red green magenta")

END

DEF F(x) = Sin(x*x)
