import('../../modules/components/popStateListener.js');

export function testPopStateListener(logger) {
	// do nothing.
	// We just want to verify that no JavaScript errors are thrown by importing the module.
};